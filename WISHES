DOC/MAN

  > - man page vhset.1 (en)
    - man linlib (en)

LINLIB, VHSET, 

    - наследование от stty cooked: intr, erase, werase, kill, eof
    - ^Z для SYSV (ISC) lin132/lin140 must be redefined, also Del/BS, Ctl-W, Ctl-U

    -- навести порядок с инициализацией vexdir - см. пример vtest/pmain.c
    
LBP+VCC/VLBP

    -+ line.h: INP(LPA) -
	пересмотреть концепцию второго ряда логических атрибутов (для ввода) -
	// (реально нужны только VAR,MSE и для красоты ALT отличается от VAR)
	// остальные LPA можно использовать для сообщений, и индикаторов,
	// но сейчас они странно называются и это источник путаницы;


VASH
     - history:

    -? special case for cd command history
    - separate shorthand cache and log(conduit) files
    - compatible with .bash_history (timestamp)

 -+ rescan dir: keep cursor near last known item,
         -- keep last item name regarding -@ parameter - after rescan
	 // it's required more sofisticated comparizon (ex., after 'rm -f ##')

    -- VASH_LABEL fill from header (1st line) of commands like ps, df

    -? add flag to argv "jump to directory"
    - extended file attributes (<rwx> unsufficient) permissions check 
    - work with very big directories and inode numbers
      (without static constant allocation for main meny storage, with malloc+realloc)

    - cmd menu: TAB completion must show next frame of candidates
    -- main menu: filter on command '_', without fresh read
    -- main menu: mode stat on objects driven by flags (now :Fa)
    -- main menu: render view more compact (find max wifth on every column)
    
 *  - f2:menu - rework to support std-profiles

    - Часы - убрать секунды (или сделать опцию)
      -- WTF: #define CP_SAV


