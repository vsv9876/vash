
	LINLIB

- inheritance from stty cooked:
   intr, erase, werase, kill, eof, tstop

- revive linlib/visi library as a separate project

- man linlib (en) + actualize russian versions

	VISI (LBP,VCC,VLBP)

- filler/starter character of line should be [~_#], not limited by [.]

	VASH

- history:
-- separate shorthand cache(history) and log(conduit) files;
-- make the conduit compatible with .bash_history (timestamp support required)

- substitution some metasymbols before internal commands

-+ rescan dir: keep cursor position near last known item,
-- keep last item name regarding -@ parameter (item name fragment)
   after new rescan
   // this feature requires more sofisticated comparizon of menu items
   // (ex., after 'rm -f ##')

-- VASH_LABEL fill from header (1st line) of commands like ps, df, 
   then mark this line in main menu as read-only
 
-- extended file attributes permissions check (<rwx> unsufficient) 

-? work with very big directories and inode numbers
   (realloc vs constant static allocation)

-- more compact rendering of main menu - maximum width of item
   must be recalculated on every column
   (now common for all columns -- since 'comint'-compatible)

