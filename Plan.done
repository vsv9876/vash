
------------------------------------------------------------------------------
    
    -- LINLIB, VHSET, 

    +- ограничение вывода на экран по maxli,maxco: cp_set,w_chr
    +- поддержка панелей - спец.версии cp_set,at_set,w_panel
    
    + редактор "бесконечныx" строк в ограниченном окне -
    //- r_line(), line.h, vcc/vlbp -- интегрировать
    //+ e_str/w_str: контроль границ буфера строки (4K> bytes) -- реализован
    ++ support for wsobj_t in r_line/w_line
    +- UTF-8: implement wcwidth() into editors r_line(),e_str()

    +- флаги управления режимами переделать на структуру или массив
 
 +  -- ANSI Color SGR 8/16
    ++ навести порядок vhset с нулевой страницей настройки (основные клавиши) -
	    // 4 кнопки стрелок переопределять _никогда_ не требуется
    ++ синонимы клавишных команд (сейчас спрятаны)
    ++ новые страницы справки HELP PAGES rewrite/reduce
   +- color16 support (see MEMO in WISHES)
   	-- add rendering for 90-97 and 100-107 SGR codes
   	-- add 2 rows of flags in visual attributes page in vhset 

    + line.h: INP(LPA) -
	пересмотреть концепцию второго ряда логических атрибутов (для ввода) -
	// реально нужны только VAR,MSE и для красоты ALT
	// остальные LPA можно использовать для сообщений, и индикаторов,
	// но сейчас они странно называются и это источник путаницы;	
    + добавлен "забытый" CMD
    ------------------------------------------------------------------------------

    -- VASH:
  + trap like on <Enter> for all Ctrl-* including Ctrl-C
  *>  - refactoring: convert single flags into array or structure
  *>  - history, history search:
	+- simple pattern via menu (highlight, select) 

  +  - оптимизация файлов настройки (директива include?)
 ++ VAPATH,VEXDIR like PATH

* completion support
    + man
********************************************************************
