- прочистить .ashmenu или сделать варианты BSD/SYSV

+ ПРОВЕРИТЬ SHELL = /bin/sh во всех Makefile,
  make clean (vhset), make install vhset/visi

- проверить правила в Makefiles от препроцессоров visi

+ Избавиться от ld -r для ash/libnew/libnew.o

+ доделать install для visi/vhset - visilib

- suspend должен работать не только при SHELL=/bin/csh
- ^Z для SYSV (ISC) lin132/lin140 должен переопределяться

- настройка LINES, COLUMNS должна быть встроена в ash

+ объединить ashrc на разных платформах в один текст, 
  uname || hostname > /dev/null ???

- restricted ash version, without command line editing/confirmation

- общие настройки для .ashstd, и проч. .ash* через встроенный препроцессор

- команды для редактирования строки и переходов между контекстами
  должны быть более удобными, желательно выбор vi/emacs/pico

+ добавить регулирование предпочтений, сейчас только EDITOR, PAGER
- надо еще MAILVIEW, MAILEDIT

- command history shareable on multiply sessions, like ksh,
- search pattern across history

- наследование от stty cooked intr, erase.

+?   проверки на доступность /dev/tty, для сброса зависаний...

+ Диагностика про неопознанные клавиши должна выглядеть так:
  имя файла настройки: клавиша неопределена (возможно, в termcap)

+ Распознавание и настройка env LINES, COLUMNS

+ Сливание версий SYSV & 386bsd

- Поддержка работы с двумя каталогами

- Часы - убрать секунды (или сделать опцию)

+ Вывеска клавиш и pwd - при выключенной нижней панели
  pwd рисовать внизу в последней строке.

+ Мульти-групп проверки прав доступа (для файловой системы BSD & SVR4)

- запуск из каталога, открытого только на поиск,
  не должен приводить к core dumped, разрешить вход в каталоги, открытые
  только на чтение, например drw-rw-rw- - такое бывает при монтировании
  флоппи DOS на UnixWare ;-)
- и вообще добавить флаг "при старте перейти в каталог такой-то"...

- добавить декорации для xterm, устанавливаемые из ash, и управляемые setup
