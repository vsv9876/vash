- разобраться, почему перестал работать (вызываться) _cmdset
+ exec vash не должен скролить место, которое он занимает

-+ переименовать проект, в том числе, префиксы имен настроечных файлов
  + vash OK, но остались .ashstd и др.

-- Unicode/UTF-8 support for main list (filenames) and internas



--?? переставить второй символ имени объекта (файла) в конец, как у ls -F
--?? переделать встроенную команду ls (:Fa) - список получать любой командой,
  стандартно встроенным ls, но получать символ типа (второй символ имени объекта) встроенной функцией (будет показываться в конце имени объекта)
--?? функция получения символа типа должна быть отключаемой - 
    объекты не обязательно файлы


linlib/vhset:
-- навести порядок vhset с нулевой страницей настройки (основные клавиши) -
   похоже что 4 кнопки стрелок переопределять _никогда_ не требуется,
-- но должен быть выбор "языков" навигации/редактирования -
? наверное, команды для редактирования строки и переходов между контекстами
  должны быть более удобными, желательно выбор linlib/vi/emacs
-- vhset: настройка синонимов должна быть явной (сейчас она "спрятана")
+ vhset: расширить набор клавишных команд PgUp, PgDown, Home, End
+ linlib: расширить набор операций редактирования

-+?- реинкарнация - перезапуск vash в том же окружении, с возобновлением контекста приложения
+- перехват изменения размеров окна терминала (LINES, COLUMNS) встроить в ash -
  вызывается явно, путем рестарта vash

?- restricted ash version, without command line editing/confirmation
?- общие настройки для .ashstd, и проч. .ash* через встроенный препроцессор
- оптимизация файлов настройки (директива include?)
- прочистить .ashmenu или сделать варианты BSD/SYSV

-+? suspend должен работать не только при SHELL=/bin/csh
- ^Z для SYSV (ISC) lin132/lin140 должен переопределяться

- linlib/vhset: наследование от stty cooked intr, erase.
+? linlib/vhset: проверки на доступность /dev/tty, для сброса зависаний...

+ запуск команды $SHELL -c 'cmd' (имя SHELL брать из окружения)

+ history:
  command history shareable with multiply sessions, like ksh
- searching pattern across history (or select like in main lineset)
  separate parameterized command history cache and command history log
- История команды cd (меню и подстановка параметров)
- фильтр для показа в главном окне (по команде '_') 
+ command completion - at least, 'cd' and command from $path
-? добавить флаг "при старте перейти в каталог такой-то"...
- Поддержка проверки расширенных атрибутов файлов (rwx маловато будет)
- работа с очень большими каталогами и номерами inode (уйти от статического выделения памяти под списки главного окна)

+ починить меню F2, сейчас там какие-то заглушки (это были проблемы malloc)
+ навигатор окна vf --==- не должен выходить за границы одной строки

+ добавить регулирование предпочтений, сейчас только EDITOR, PAGER
?- надо еще MAILVIEW, MAILEDIT
- Часы - убрать секунды (или сделать опцию)

- запуск из каталога, открытого только на поиск,
  не должен приводить к core dumped, разрешить вход в каталоги, открытые
  только на чтение, например drw-rw-rw- - такое бывает при монтировании
  флоппи DOS на UnixWare ;-)

