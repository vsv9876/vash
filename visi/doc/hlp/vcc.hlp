/*      +----------+    библиотека ввода-вывода */
/*     (c) linlib  !    для алфавитно-цифровых  */
/*      +----------+    видеотерминалов         */

#include "line.h"

---PAGE    page
---PORTS        ---LINES        ---SCREEN       ---END    ---#
---LINES
/****
				+--- * - n u d l r t (задержки)
				!                                ****/
		name =  attr    flag    cvts    cvtf    test    varl
/*			----    ----    ----    ----    ----    ----
у поля есть подсказка   p       -       -       -       -       (1
поле для ввода          i

комментарий             c                                       "текст"
центрируемый заголовок  h                                       "текст"
строка символов         v               -       -       tst     s
переменная через формат v               "%M.Nf" -       tst     &v
нестандартный формат    v               -       cvt     tst     &v       (2
переключатель           a               idxp    cvt_a   tst     &idxv    (3
пункт меню              m               -       proc_m  tst_m   name_m   (3
---------------------   ----            ----    ----    ----    ----
Примечания:
(1 первый и последний аргумент обязательно указан
(2 поле cvts можно задействовать для дополнительной информации
(3 все поля, кроме последнего, можно использовать по своему усмотрению
*/

char    s[];            /* строка символов */
float   v;              /* переменная любого типа, здесь - float */
int     tst();          /* тестирующая функция, указывать необязательно */
int     cvt();          /* функция форматного преобразования */
int     idxv;           /* индекс переключателя, а также:         */
char   *idxp[];         /* - текстовые названия каждого значения, */
int     cvt_a();        /* - библиотечная функция поддержки.      */

int     proc_m();       /* функция, которую нужно вызвать, если    */
char    name_m[];       /*        указан этот пункт */
int     tst_m();        /*   тестирующая функция для меню из библиотеки */

vmain() {
	LINE *cline;    /* УКАЗАТЕЛЬ НА ТЕКУЩУЮ ЛИНИЮ */
	int   cpos;     /* ТЕКУЩАЯ ПОЗИЦИЯ ПРИ ПЕРЕХОДЕ В РЕДАКТОР СТРОКИ */

	w_line( &line);   r_line( &line, &cpos );
	w_page( page );   r_page( page, &cline, &cpos );
}

tst(line, cod)
LINE *line;  kbcod cod; {
	return(TRUE);   /* введено правильное значение */
	return(FALSE);  /* что-то не так, повторить ввод */
}
