.TH VLBP 1 VISI
.SH ИМЯ
vlbp, visi \- Препроцессор вынесенных описаний страниц
.SH
.ES
 +----------+   БИБЛИОТЕКА ВВОДА-ВЫВОДА
(c) linlib  !   ДЛЯ АЛФАВИТНО-ЦИФРОВЫХ
 +----------+   ВИДЕОТЕРМИНАЛОВ
.EE
.SH ФОРМАТ
.ES 5
 vlbp [-o file.lb] file.lav
     file.lav - описание страницы в стиле visi
     file.lb  - описание страницы в формате lbp

.EE

.SH ОПИСАНИЕ
.PP
Команда входит в инструментальный комплекс LINLIB,
служит для подготовки вынесенных описаний страниц.
По умолчанию результат обработки попадает
на стандартный вывод (для отладки).
В результате работы программы получается файл в формате
lbp, который нужно обработать компилятором lbp.
.PP
Описание страницы помещается в файл с суффиксом ".lav".
Для выделения описания используется набор ключевых слов,
которые понимает препроцессор vlbp.
Собственно форму страницы, то есть ее вид на экране,
нужно просто "нарисовать" при помощи обычного редактора текстов,
например red.
.PP
Описание страницы состоит из секций, которые выделяются
ключевыми словами. Порядок секций произвольный. Для vlbp реализованы
следующие секции:
.JP "---PAGE"
Начало описания страницы.
.JP "---END"
Конец описания страницы. Все остальные секции помещаются
между ключевыми словами
.EX ---PAGE
.EX ---END
.JP "---SCREEN"
Секция содержит линии, из которых состоит страница, включая
комментарии и поля для ввода. Подробное описание этой и остальных
секций см. visi(3x).
.JP "---LINES"
В этой секции помещаются описания линий, для которых недостаточно
умолчания, например, если необходимо указать форматное преобразование или
флаг задержки.
.JP "---TABLES"
Описание таблиц, точнее смещений для построения таблиц.
.SH ЗАМЕЧАНИЯ
Имена в секции
.EX ---LINES
могут иметь не более 6 знаков символьного кода,
Остальные просто обрезаются компилятором lbp.
.SH ДОПОЛНИТЕЛЬНЫЕ ССЫЛКИ
ldemo(1), linlib(3x), vcc(1), visi(3x), lin3(3x), lin4(3x).
.SH ДИАГНОСТИКА
Препроцессор пытается обнаружить ошибки в описании страницы,
формат выдачи диагностики такой же, как у компилятора Си.
Не забывайте, что описание страницы до и после обработки vlbp
занимает разное количество строк.
