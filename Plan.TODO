
BUGS/FIX

!! - не сбрасываются переназначения > < при перечитывании основного меню,
  сейчас только после выполнения команды!!

! -- лишнее перечитывание главного меню, например std_help.rc (но третий раз не перечитывает) 

DOC/MAN
    ------------------------------------------------------------------------------
    
LINLIB, VHSET, 

  -- r_page() navigation with PgUp/PgDn/Home/End

    -- статусная строка (трей приложения)
	
    -- r_key(): correct input for non-unicode 8-bit encodings
    
    -- support for ^U ^W - erase all, erase word
    
  -- support for wsobj_t in r_line/w_line
 
    
 >>-- BUG: строка strobj_t обрезается на старте редактирования команды
   -- редактор "бесконечныx" строк в ограниченном окне -
            //- r_line(), line.h, vcc/vlbp -- интегрировать
	    //+ e_str/w_str: контроль границ буфера строки (4K> bytes) -- реализован

LBP+VCC/VLBP

    -- добавить атрибуты LBP до полного набора атрибутов (для раскраски страниц help минимум)
   >-- filler character must be [~_#], not limited by [.]
   >-- wcwidth > 1 support for ---SCREEN section

    ------------------------------------------------------------------------------

VASH:
    - setup flag: mask ExCI for ;: variants of execution
    - setup flag: sh -c only 
    
    -- list main menu with PgUp/PgDown/Home/End

    -- substitution #@ on manual editing cmd
    
*>  - history, history search:
	-- simple pattern via ^R (highlight, select) 
	-- rename .ashhist to .history.vash
		-- store format 'timestamp,usageCount,protected,cmd'

*  -+?- export vash flags changed in setup, on execve() with VASH= environment

* completion support
    -- man
    -- make
    
