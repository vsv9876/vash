
    -- BUGS/FIX

! лишняя терминация бесконечных строк внутри r_line/w_line

    -- DOC/MAN

  > - man page vhset.1 (en)
    - man linlib (en)
    ------------------------------------------------------------------------------
    
    -- LINLIB, VHSET, 

  >*  -- новая навигация r_page(), более каноническая - PgUp/PgDown, Home/End
    -- статусная строка (трей приложения)
	

    -- Unicode/UTF-8 support:
      implement wcwidth() into w_wchr() and editor
 
    -- навести порядок с инициализацией vexdir - см. пример vtest/pmain.c
    
 >>-- BUG: строка strobj_t обрезается на старте редактирования команды
   -- редактор "бесконечныx" строк в ограниченном окне линии -
            //- r_line(), line.h, vcc/vlbp
	    //- e_str/w_str: контроль границ буфера строки (4K bytes),

    -+  ANSI Color SGR/256

    -+ line.h: INP(LPA) -
	пересмотреть концепцию второго ряда логических атрибутов (для ввода) -
	// реально нужны только VAR,MSE и для красоты ALT
	// остальные LPA можно использовать для сообщений, и индикаторов,
	// но сейчас они странно называются и это источник путаницы;	
    - LBP/VLBP: добавить атрибуты до полного набора (для раскраски страниц)
 >- LBP/VLBP/VCC: filler char must be [~_#], not limited by [.]

    - наследование от stty cooked: intr, erase, werase, kill, eof
    - ^Z для SYSV (ISC) lin132/lin140 must be redefined, also Del/BS, Ctl-W, Ctl-U
    ------------------------------------------------------------------------------

    -- VASH:
*>  - refactoring: convert single flags into array with constants #define in assist.h
	- setup flag: mask ExCI for ;: variants of execution
	- setup flag: sh -c only 
    
    VASH history:
*>  - navigate on PgUp, etc
 *  - list main menu with PgUp/PgDown/Home/End
    -? special case for cd command history
    - separate cache and log(conduit) files
    - compatible with .bash_history (timestamp)
*>  - search simple pattern via menu and ^R

    - cmd menu: TAB completion must show next frame of candidates
    -- main menu: filter on command '_'
    -- main menu: mode stat on objects driven by flags (now :Fa)
    -- main menu: render compact //count width in every column

 *  -+?- pass vash flags on execve() as environ

 -+ rescan dir: keep cursor near last known item,
    //// it is need more sofisticated comparizon (ex., rm -f ##)
    -- keep last item name regarding -@ parameter - after rescan
    -- mavigation r_page() on PgUp/PgDn/Home/End
    -- rendering main view more compact, find max on every column
    -- VASH_LABEL fill from header (1st line) of commands like ps, df

    argv/argc call:
    -? add flag to argv "jump to directory"
    - extended file attributes (<rwx> unsufficient) permissions check 
    - work with very big directories and inode numbers (уйти от статического выделения памяти под списки главного окна) (malloc+realloc)

 *  - f2:menu - rework to support std-profiles

    - Часы - убрать секунды (или сделать опцию)
      -- WTF: #define CP_SAV
********************************************************************
    -- MEMO
    -   a=F find . ! -path \*/.git/\* -type d | sed s,^./,, 
    
    - $ man rpm|egrep "^[ \t]+-([^ ])+" | sed 's/ /\n/g' | grep -e ^- |sort -u | less

    - 中文素养    国际站 == vtest
